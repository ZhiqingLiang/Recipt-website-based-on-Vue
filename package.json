{
  
  "devDependencies": {
  "concurrently": "^8.2.2"
  },
  "name": "project",
  "version": "1.0.0",
  
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "frontend":"cd frontend && npm start",
  "backend":"cd backend && node serve.js",
  "start": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
